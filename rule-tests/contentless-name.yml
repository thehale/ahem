# Copyright (c) Joseph Hale, 2026
# SPDX-License-Identifier: MPL-2.0

Bash:
  invalid:
    - "data=1"
  valid:
    - "attempts=1"
C:
  invalid:
    - "int data = 1;"
  valid:
    - "int attempts = 1;"
Cpp:
  invalid:
    - "int data = 1;"
  valid:
    - "int attempts = 1;"
CSharp:
  invalid:
    - "var data = Load();"
  valid:
    - "var invoices = Load();"
Dart:
  invalid:
    - "var data = load();"
  valid:
    - "var invoices = load();"
Elixir:
  invalid:
    - "result = compute()"
  valid:
    - "duration = compute()"
Go:
  invalid:
    - |
      package m
      var temp = 1
  valid:
    - |
      package m
      var attempts = 1
Hcl:
  invalid:
    - "data = 1"
  valid:
    - "attempts = 1"
Java:
  invalid:
    - "class A { int data = 1; }"
  valid:
    - "class A { int attempts = 1; }"
JavaScript:
  invalid:
    - "const data = load();"
    - "let result = 1;"
  valid:
    - "const invoices = load();"
    - "let total = 1;"
Kotlin:
  invalid:
    - "val data = load()"
  valid:
    - "val invoices = load()"
Lua:
  invalid:
    - "local data = load()"
  valid:
    - "local invoices = load()"
Nix:
  invalid:
    - "{ data = 1; }"
  valid:
    - "{ attempts = 1; }"
Php:
  invalid:
    - |
      <?php
      $data = load();
  valid:
    - |
      <?php
      $invoices = load();
Python:
  invalid:
    - "value = compute()"
  valid:
    - "duration = compute()"
Ruby:
  invalid:
    - "result = compute"
  valid:
    - "duration = compute"
Rust:
  invalid:
    - "fn f() { let obj = g(); }"
  valid:
    - "fn f() { let listener = g(); }"
Scala:
  invalid:
    - "val data = load()"
  valid:
    - "val invoices = load()"
Solidity:
  invalid:
    - "contract A { uint data; }"
  valid:
    - "contract A { uint attempts; }"
Swift:
  invalid:
    - "let data = load()"
  valid:
    - "let invoices = load()"
Tsx:
  invalid:
    - "const data = load();"
  valid:
    - "const invoices = load();"
TypeScript:
  invalid:
    - "const data = load();"
  valid:
    - "const invoices = load();"
